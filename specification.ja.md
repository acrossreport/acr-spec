# ACR テンプレート仕様 v2.0

**ACR (Across Report)** — プリンタ非依存・セクションベース帳票テンプレート仕様

> **ステータス:** ベータ
> この仕様は最初の安定版リリースまで変更される可能性があります。

---

## 目次

1. [目的](#1-目的)
2. [スコープ](#2-スコープ)
3. [設計原則](#3-設計原則)
4. [用語定義](#4-用語定義)
5. [座標系と単位](#5-座標系と単位)
6. [ページモデル](#6-ページモデル)
7. [セクションモデル](#7-セクションモデル)
8. [セクションレンダリング順序](#8-セクションレンダリング順序)
9. [ページネーションルール](#9-ページネーションルール)
10. [グループ処理ルール](#10-グループ処理ルール)
11. [明細処理ルール](#11-明細処理ルール)
12. [コントロール](#12-コントロール)
13. [コントロール共通プロパティ](#13-コントロール共通プロパティ)
14. [ラベルコントロール](#14-ラベルコントロール)
15. [フィールドコントロール](#15-フィールドコントロール)
16. [ラインコントロール](#16-ラインコントロール)
17. [矩形コントロール](#17-矩形コントロール)
18. [計画中のコントロール](#18-計画中のコントロール)
19. [データモデル](#19-データモデル)
20. [フィールド解決ルール](#20-フィールド解決ルール)
21. [フォーマットルール](#21-フォーマットルール)
22. [テンプレートルート構造](#22-テンプレートルート構造)
23. [規範的レンダリング制約](#23-規範的レンダリング制約)
24. [出力モデル](#24-出力モデル)
25. [最小構成サンプル](#25-最小構成サンプル)
26. [拡張サンプル](#26-拡張サンプル)
27. [互換性ポリシー](#27-互換性ポリシー)
28. [対象外事項](#28-対象外事項)
29. [バージョニングポリシー](#29-バージョニングポリシー)
30. [変更履歴](#30-変更履歴)

---

## 1. 目的

ACR は、固定レイアウト帳票を JSON で定義するためのプリンタ非依存帳票テンプレート仕様です。

以下の関心事を分離することを目的としています：

* テンプレート構造
* データバインディング
* レイアウト処理
* 出力レンダリング

同一のテンプレートとデータセットは、帳票定義を変更することなく複数の出力形式にレンダリング可能でなければなりません。

---

## 2. スコープ

本仕様が定義する範囲：

* ページモデル
* セクションモデル
* コントロールモデル
* データバインディング動作
* ページネーション動作
* グループブレーク動作
* レンダリング順序
* 出力の期待動作

本仕様が定義しない範囲：

* ビジュアルデザイナーの実装
* エディタUI動作
* プリンタドライバ動作
* OS固有のフォント代替ルール
* サブレポートの実行

---

## 3. 設計原則

### 3.1 プリンタ非依存

ACR テンプレートは特定のプリンタドライバに依存してはなりません。

### 3.2 固定レイアウト

ACR は固定レイアウトモデルです。
すべてのコントロールは絶対座標を使用します。
HTMLのようなフローレイアウトや自動リフローはありません。

### 3.3 セクションベース処理

ACR は従来の帳票エンジンと互換性のあるバンド型セクション構造を使用します。
帳票は決定論的な垂直シーケンスでセクションを処理することで構築されます。

### 3.4 データとレイアウトの分離

テンプレートJSONがレイアウトを定義します。
データJSONが行の値を定義します。
レンダラーは実行時に両者を結合します。

### 3.5 決定論的レンダリング

同一のテンプレート・データ・レンダリング環境が与えられた場合、エンジンは同一のレイアウト結果を生成しなければなりません。

---

## 4. 用語定義

| 用語 | 意味 |
|---|---|
| テンプレート | ページ・セクション・コントロールを記述するJSONドキュメント |
| データ | 行データを提供するJSONドキュメント |
| ページ | 1つの論理出力ページ |
| セクション | 帳票レイアウト内の1つのバンド |
| コントロール | セクション内の1つの描画要素 |
| 明細 | 各データ行に対してレンダリングされる繰り返しセクション |
| グループ | フィールド値に基づく論理的な行グループ |
| 印刷可能領域 | 余白を適用した後に残るページ領域 |
| 現在行 | 現在レンダリング中のデータ行 |
| 次行 | ブレーク検出に使用される次のデータ行 |

---

## 5. 座標系と単位

### 5.1 単位

ACR は **twip** を標準座標単位として使用します。

* 1インチ = 1440 twip
* 1ポイント = 20 twip
* 1mm ≈ 56.69 twip

ページサイズ・余白・セクション高さ・コントロール境界はすべて twip で表現されます。

### 5.2 原点

座標の原点は含まれる領域の左上隅です。

* X は左から右に増加
* Y は上から下に増加

### 5.3 座標空間

* ページレベルの値は論理ページからの相対値
* セクションのコントロール座標はセクション左上からの相対値

### 5.4 数値

実装は整数または浮動小数点数値を受け入れることができます。
エンジンはレンダリングのために内部で丸め処理を行うことがありますが、レイアウトの解釈は論理的に安定していなければなりません。

---

## 6. ページモデル

ページオブジェクトは論理ページサイズと余白を定義します。

```json
{
  "page": {
    "width": 11906,
    "height": 16838,
    "margin_top": 1440,
    "margin_bottom": 1440,
    "margin_left": 1440,
    "margin_right": 1440
  }
}
```

### 6.1 ページフィールド

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `width` | number | Yes | ページ幅（twip） |
| `height` | number | Yes | ページ高さ（twip） |
| `margin_top` | number | No | 上余白（twip） |
| `margin_bottom` | number | No | 下余白（twip） |
| `margin_left` | number | No | 左余白（twip） |
| `margin_right` | number | No | 右余白（twip） |

### 6.2 印刷可能領域

印刷可能領域の計算：

* 印刷可能幅 = ページ幅 - 左余白 - 右余白
* 印刷可能高さ = ページ高さ - 上余白 - 下余白

すべてのセクション配置は印刷可能領域内で行わなければなりません。

---

## 7. セクションモデル

ACR はセクションベースの構造を使用します。
セクションは固定高さとコントロールのリストを持つ垂直バンドです。

### 7.1 サポートするセクションタイプ

* `report_header`（レポートヘッダ）
* `page_header`（ページヘッダ）
* `group_header`（グループヘッダ）
* `detail`（明細）
* `group_footer`（グループフッタ）
* `page_footer`（ページフッタ）
* `report_footer`（レポートフッタ）

### 7.2 セクションの基本

各セクションは以下を持ちます：

* タイプ
* 固定高さ
* 0個以上のコントロール

例：

```json
{
  "height": 600,
  "controls": []
}
```

### 7.3 セクションの制約

* セクションは自由浮動バンドとしてではなく、垂直に処理されます
* セクション高さはレイアウト中に固定されます
* コントロールは正確に1つのセクションに属します
* セクション構造はレンダリング中に書き換えてはなりません

---

## 8. セクションレンダリング順序

帳票の論理レンダリング順序：

1. `report_header` — 帳票開始時に1回
2. `page_header` — 各ページの先頭
3. 0個以上の `group_header` — グループ開始時
4. `detail` — 各データ行
5. 0個以上の `group_footer` — グループ終了時
6. `page_footer` — 各ページの末尾
7. `report_footer` — 帳票終了時に1回

### 8.1 最初のページ

定義されている場合、`report_header` は最初の明細行より前に処理されます。
`page_header` は最初のページでも処理されます。

### 8.2 最後のページ

定義されている場合、`report_footer` はデータ依存の最後のセクションの後に処理されます。

---

## 9. ページネーションルール

ページネーションはセクションベースです。
セクションは現在のページに十分な残余スペースがある場合にのみ配置されます。

### 9.1 残余高さ

残余高さは、既に配置されたセクションと該当する場合のフッタ予約スペースを考慮した後の印刷可能領域内で計算されます。

### 9.2 改ページ条件

次に必要なセクションが利用可能な残余高さに収まらない場合、新しいページを開始しなければなりません。

### 9.3 ページヘッダとフッタの動作

* `page_header` は定義されている場合、すべてのページでレンダリングされます
* `page_footer` は定義されている場合、すべてのページでレンダリングされます

### 9.4 明示的な改ページ

実装においてセクションに `new_page` 動作フラグがある場合、エンジンはそのセクションをレンダリングする前に新しいページを開始しなければなりません。

### 9.5 セクションの分割禁止

セクションは単一のレイアウト単位として扱われます。
実装が将来的な特別な動作を明示的に定義しない限り、複数のページにまたがって分割してはなりません。

---

## 10. グループ処理ルール

グループはフィールド値の遷移に基づきます。

### 10.1 グループ定義

グループはフィールド名で定義されます。

例：

```json
{
  "group_data_field": "customer_code"
}
```

### 10.2 グループオープン

グループは以下の場合に開きます：

* 最初の行の処理が開始された場合、または
* 現在行のグループ値が前行のグループ値と異なる場合

### 10.3 グループブレーク

グループブレークは以下の場合に発生します：

* 現在行の値が次行の値と異なる場合、または
* 次行が存在しない場合

### 10.4 グループヘッダ

グループが開く場合、対応する `group_header` は、そのグループに属する行の明細より前にレンダリングされなければなりません。

### 10.5 グループフッタ

グループが閉じる場合、対応する `group_footer` は、そのグループに属する最後の明細行の後にレンダリングされなければなりません。

### 10.6 ネストされたグループ

複数のグループがサポートされている場合、オープン時は外側から内側へ、クローズ時は内側から外側へ処理しなければなりません。

---

## 11. 明細処理ルール

### 11.1 明細の繰り返し

`detail` セクションは `rows` の各行に対して1回レンダリングされます。

### 11.2 行の順序

行はソース順に処理されます。
ACR は仕様自体にソート動作を定義しません。
ソートが必要な場合は、レンダリング前またはエンジン固有の拡張によって適用しなければなりません。

### 11.3 空のデータセット

`rows` が空の場合：

* `detail` はレンダリングされません
* グループセクションはレンダリングされません
* レポートレベルおよびページレベルのセクションはエンジンポリシーに応じてレンダリングされる場合があります

安定した動作のために、実装は空の帳票の処理方法をドキュメント化すべきです。

---

## 12. コントロール

コントロールはセクション内に配置される描画オブジェクトです。

現在サポートされているコントロール：

* `label`（ラベル）
* `field`（フィールド）
* `line`（ライン）
* `rectangle`（矩形）

計画中のコントロール：

* `image`（画像）
* `barcode`（バーコード）

---

## 13. コントロール共通プロパティ

すべてのコントロールは以下の基本プロパティを共有します。

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `type` | string | Yes | コントロールタイプ |
| `x` | number | Yes | セクション内の左位置 |
| `y` | number | Yes | セクション内の上位置 |
| `width` | number | Yes | コントロール幅 |
| `height` | number | Yes | コントロール高さ |

### 13.1 境界

コントロール座標は含まれるセクションからの相対値です。

### 13.2 オーバーフロー

本仕様は自動拡大・自動縮小の動作を定義しません。
テキストが利用可能な境界を超える場合の動作は、後で拡張されない限り実装定義です。

---

## 14. ラベルコントロール

ラベルはテンプレートに直接定義された静的テキストをレンダリングします。

```json
{
  "type": "label",
  "x": 0,
  "y": 0,
  "width": 3000,
  "height": 400,
  "text": "請求書",
  "font_name": "Noto Sans JP",
  "font_size": 24,
  "bold": true,
  "italic": false,
  "color": "#000000",
  "align": "left",
  "valign": "top"
}
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `text` | string | Yes | 静的テキスト |
| `font_name` | string | No | フォントファミリー |
| `font_size` | number | No | フォントサイズ（ポイント） |
| `bold` | boolean | No | 太字 |
| `italic` | boolean | No | 斜体 |
| `color` | string | No | テキスト色（`#RRGGBB`） |
| `align` | string | No | `left`・`center`・`right` |
| `valign` | string | No | `top`・`middle`・`bottom` |

---

## 15. フィールドコントロール

フィールドは現在行から解決されるデータバインドテキストをレンダリングします。

```json
{
  "type": "field",
  "x": 0,
  "y": 0,
  "width": 3000,
  "height": 400,
  "field": "customer_name",
  "font_size": 12,
  "align": "left",
  "format": ""
}
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `field` | string | Yes | データキー名 |
| `font_name` | string | No | フォントファミリー |
| `font_size` | number | No | フォントサイズ（ポイント） |
| `bold` | boolean | No | 太字 |
| `italic` | boolean | No | 斜体 |
| `color` | string | No | テキスト色 |
| `align` | string | No | `left`・`center`・`right` |
| `valign` | string | No | `top`・`middle`・`bottom` |
| `format` | string | No | フォーマット指定 |

---

## 16. ラインコントロール

ラインはコントロール境界を使用して直線をレンダリングします。

```json
{
  "type": "line",
  "x": 0,
  "y": 0,
  "width": 5000,
  "height": 0,
  "color": "#000000",
  "thickness": 20
}
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `color` | string | No | 線の色 |
| `thickness` | number | No | 線の太さ |

水平線：`height = 0` を使用
垂直線：`width = 0` を使用

---

## 17. 矩形コントロール

矩形は枠線および/または塗りつぶしの矩形をレンダリングします。

```json
{
  "type": "rectangle",
  "x": 0,
  "y": 0,
  "width": 5000,
  "height": 800,
  "fill_color": "#F5F5F5",
  "border_color": "#CCCCCC",
  "border_thickness": 15,
  "radius": 0
}
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `fill_color` | string | No | 塗りつぶし色または `none` |
| `border_color` | string | No | 枠線色または `none` |
| `border_thickness` | number | No | 枠線の太さ |
| `radius` | number | No | 角の丸み |

---

## 18. 計画中のコントロール

### 18.1 画像

画像は計画中ですが、現在の安定したコントロールセットでは規範的ではありません。

### 18.2 バーコード

バーコードは計画中ですが、現在の安定したコントロールセットでは規範的ではありません。

計画中のコントロールは、明示的に実装されない限り、現在のエンジンで必須動作として扱ってはなりません。

---

## 19. データモデル

データドキュメントは行ベースです。

```json
{
  "rows": [
    {
      "customer_name": "株式会社サンプル",
      "invoice_no": "INV-001",
      "amount": 150000
    }
  ]
}
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `rows` | array | Yes | 行オブジェクトの配列 |

各行はキーと値のマップです。

---

## 20. フィールド解決ルール

### 20.1 明細セクションの解決

`detail` では、`field` コントロールは現在行に対して解決されます。

### 20.2 明細以外の解決

レポートレベルおよびページレベルのセクションで実装がフィールドベースのコントロールを許可する場合、エンジンの現在行コンテキストを使用して解決されます。
一般的なポリシーは利用可能な場合に最初の行を使用することです。
そのポリシーはエンジンによってドキュメント化されなければなりません。

### 20.3 フィールド未存在

指定されたフィールドキーが存在しない場合、レンダリングされる値は空文字列として扱わなければなりません。

### 20.4 Null値

フィールド値がnullの場合、エンジン固有のフォーマットルールで別途指定されない限り、空文字列としてレンダリングすべきです。

---

## 21. フォーマットルール

フィールドコントロールはフォーマットをサポートすることがあります。

例：

* `#,##0`
* `#,##0.00`
* `YYYY-MM-DD`

本仕様は `format` プロパティの存在を定義しますが、フォーマット文法が完全に標準化されるまで、正確なフォーマットトークンの動作はエンジン固有のままとなる場合があります。

---

## 22. テンプレートルート構造

テンプレートルートはページ定義とセクション定義を含みます。

```json
{
  "version": "2.0",
  "page": {
    "width": 11906,
    "height": 16838,
    "margin_top": 1440,
    "margin_bottom": 1440,
    "margin_left": 1440,
    "margin_right": 1440
  },
  "report_header": { },
  "page_header": { },
  "detail": { },
  "page_footer": { },
  "report_footer": { }
}
```

### 22.1 ルートフィールド

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `version` | string | Yes | 仕様バージョン |
| `page` | object | Yes | ページ定義 |
| `report_header` | object | No | 帳票開始セクション |
| `page_header` | object | No | ページヘッダ |
| `group_header` | object または array | No | グループヘッダ定義 |
| `detail` | object | Yes | 明細セクション |
| `group_footer` | object または array | No | グループフッタ定義 |
| `page_footer` | object | No | ページフッタ |
| `report_footer` | object | No | 帳票終了セクション |

実装は単一のグループバンドまたは複数のグループバンドをサポートすることがあります。
実際に受け入れられる構造は、そのエンジンバージョンで使用されるスキーマに反映されなければなりません。

---

## 23. 規範的レンダリング制約

以下の制約は規範的です：

1. セクション順序は安定していなければなりません。
2. 既存のレイアウトロジックはテンプレートプロセッサによって暗黙的に変更されてはなりません。
3. セクション階層は帳票整合性の一部であり、保持されなければなりません。
4. ページネーションは利用可能な残余高さに基づかなければなりません。
5. 明細行は順次処理されなければなりません。
6. グループブレーク判定は値遷移ロジックに基づかなければなりません。
7. コントロールは概念的にセクション座標系の外にレンダリングしてはなりません（レンダラーが視覚的にクリップまたは丸め処理する場合でも）。

---

## 24. 出力モデル

ACR は複数の出力ターゲットをサポートすることを意図しています。
現在および計画中の例：

| 形式 | 説明 |
|---|---|
| PDF | 複数ページドキュメント出力 |
| PNGパッケージ | ページごとの画像出力 |
| ZIPパッケージ | PDFと画像などのバンドル出力 |
| ESC/POS | 計画中のサーマルプリンタ出力 |
| ZPL | 計画中のラベルプリンタ出力 |

本仕様は出力形式とは独立してレイアウト動作を定義します。

---

## 25. 最小構成サンプル

```json
{
  "version": "2.0",
  "page": {
    "width": 11906,
    "height": 16838,
    "margin_top": 1440,
    "margin_bottom": 1440,
    "margin_left": 1440,
    "margin_right": 1440
  },
  "detail": {
    "height": 600,
    "controls": [
      {
        "type": "field",
        "x": 0,
        "y": 100,
        "width": 9000,
        "height": 400,
        "field": "name",
        "font_size": 12
      }
    ]
  }
}
```

**data.json**

```json
{
  "rows": [
    { "name": "山田太郎" },
    { "name": "鈴木花子" },
    { "name": "佐藤次郎" }
  ]
}
```

---

## 26. 拡張サンプル

```json
{
  "version": "2.0",
  "page": {
    "width": 11906,
    "height": 16838,
    "margin_top": 1440,
    "margin_bottom": 1440,
    "margin_left": 1440,
    "margin_right": 1440
  },
  "report_header": {
    "height": 1200,
    "controls": [
      {
        "type": "label",
        "x": 0,
        "y": 200,
        "width": 9000,
        "height": 600,
        "text": "請求書",
        "font_size": 28,
        "bold": true,
        "align": "center"
      }
    ]
  },
  "page_header": {
    "height": 700,
    "controls": [
      {
        "type": "label",
        "x": 0,
        "y": 100,
        "width": 3000,
        "height": 400,
        "text": "得意先名",
        "bold": true
      },
      {
        "type": "label",
        "x": 3000,
        "y": 100,
        "width": 3000,
        "height": 400,
        "text": "請求番号",
        "bold": true
      },
      {
        "type": "label",
        "x": 6000,
        "y": 100,
        "width": 3000,
        "height": 400,
        "text": "金額",
        "bold": true,
        "align": "right"
      }
    ]
  },
  "detail": {
    "height": 600,
    "controls": [
      {
        "type": "field",
        "x": 0,
        "y": 100,
        "width": 3000,
        "height": 400,
        "field": "customer_name"
      },
      {
        "type": "field",
        "x": 3000,
        "y": 100,
        "width": 3000,
        "height": 400,
        "field": "invoice_no"
      },
      {
        "type": "field",
        "x": 6000,
        "y": 100,
        "width": 3000,
        "height": 400,
        "field": "amount",
        "align": "right",
        "format": "#,##0"
      },
      {
        "type": "line",
        "x": 0,
        "y": 580,
        "width": 9000,
        "height": 0,
        "thickness": 15
      }
    ]
  },
  "page_footer": {
    "height": 500,
    "controls": [
      {
        "type": "label",
        "x": 0,
        "y": 50,
        "width": 9000,
        "height": 300,
        "text": "ACR Engine",
        "align": "center",
        "font_size": 9
      }
    ]
  }
}
```

---

## 27. 互換性ポリシー

ACR は以下の点でActiveReportsなどのセクションベース帳票システムと概念的に互換性があります：

* バンド型レイアウト
* 繰り返し明細行
* ページヘッダ/フッタモデル
* グループヘッダ/フッタモデル

ただし、ACR はJSONベースの仕様として定義されており、すべてのレガシー機能を複製することを意図していません。

---

## 28. 対象外事項

以下は現在のスコープから明示的に除外されています：

* フリーフローテキストレイアウト
* ブラウザDOMレンダリングルール
* ネストされたサブレポートの実行
* 実行時のセクション再構築
* JSONに表現されないデザイナー固有の自動動作

---

## 29. バージョニングポリシー

* メジャーバージョンの変更は構造的な変更を導入することがあります。
* マイナーバージョンの変更はオプションフィールドの追加またはルールの明確化を行うことがあります。
* エンジンはレンダリング前にテンプレートバージョンを検証すべきです。

---

## 30. 変更履歴

| バージョン | 概要 |
|---|---|
| 2.0 | セクションモデル・ページネーションルール・グループルール・コントロール定義・規範的制約を含む完全仕様に再編 |
| 1.x | 初期ドラフトレベルの概要と構造定義 |
